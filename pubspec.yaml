name: appllegagt
description: Aplicación de pagos

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.29+33

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  http: ^0.13.4
  shared_preferences: ^2.0.8
  enough_convert: ^1.4.0
  barcode_scan2: ^4.1.4
  url_launcher: ^6.1.6
  webview_flutter: ^2.0.13
  flutter_dotenv: ^5.0.2
  intl: ^0.17.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true
  assets:
    - images/
    - images/icons/
    - images/logos/
    - assets/
    - .env

